dependencies {
    implementation(Libraries.LifeCycle.livedata)
    implementation(Libraries.Kotlin.reflect)
    api(Libraries.Test.Unit.junit5)
    api(Libraries.Test.Common.assertK)
    api(Libraries.Test.Common.mockk)
    api(Libraries.Test.Unit.spek)
    api(Libraries.Test.Unit.spekRunner)
    api(Libraries.Test.Unit.arch)
}